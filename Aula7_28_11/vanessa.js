function random (inferior=0, superior=1,tipo)
{

        switch(tipo)
        {
            case 'float': return Math.random();
            case 'int': return Math.floor(Math.floor()*(superior+1))
        }

    }